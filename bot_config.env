# Bot Configuration
TELEGRAM_BOT_TOKEN=TELEGRAM_BOT_TOKEN
ADMIN_IDS=123456789,987654321

# Panel API Configuration
PANEL_API_BASE_URL=https://panel.example.com

# Webhook/Polling Configuration
# تنظیم USE_WEBHOOK=true برای استفاده از webhook
# تنظیم USE_WEBHOOK=false برای استفاده از polling
USE_WEBHOOK=false
WEBHOOK_URL=https://your-domain.com
WEBHOOK_PATH=/telegram-bot/webhook
WEBHOOK_HOST=0.0.0.0
WEBHOOK_PORT=8484

# Logging Configuration
LOG_LEVEL=INFO

# Server Configuration
WEBAPP_HOST=0.0.0.0
WEBAPP_PORT=8443

# SSL Configuration (for production)
# WEBHOOK_SSL_CERT=/path/to/cert.pem
# WEBHOOK_SSL_PRIV=/path/to/private.key

# نکات مهم:
# 1. در حالت webhook، polling غیرفعال می‌شود
# 2. در حالت polling، webhook غیرفعال می‌شود
# 3. برای production، USE_WEBHOOK=true توصیه می‌شود
# 4. برای development، USE_WEBHOOK=false مناسب است